#pragma once

#include <memory>

class Event;
class ICommand;

////////////////////////////////////////////////////////////////////////////////
//
/// Интерфейс состояния
/**
*/
////////////////////////////////////////////////////////////////////////////////
class IState
{
public:
  /// Передает событие состоянию
  virtual std::unique_ptr<ICommand> OnEvent(const Event & event) = 0;
};